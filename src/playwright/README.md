# Playwright

## Requirements and Installation
- Ensure playwright is installed
`npx playwright install`

## Frontend tests
`npm run playwright-frontend`

## API tests
`npm run playwright-api`

## Test report
To view the test results html report and trace
`npm run playwright-report`

## UI mode
To run and debug tests in the dev UI mode
`npm run playwright-ui`